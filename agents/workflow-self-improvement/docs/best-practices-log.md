# Best-practices log

A running log of official guidance reviewed by the scheduled
workflow-self-improvement research cycle.

## LangGraph — human-in-the-loop
- `interrupt()` + `Command(resume=...)` is the recommended HITL approval
  pattern. Source: https://docs.langchain.com/oss/python/langgraph/interrupts
